<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Recipes</title>
  </head>
  <body>
    <h1>Recipes</h1>
    <% @recipes.each do |post| %>
    <div class="recipe">
      <p><%= post.id %>. <a href="/posts/#{post.id}"><%= post.name %></a></p><br>
    </div>
    <% end %>
  </body>
</html>
